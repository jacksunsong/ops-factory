interface:
  display_name: "Simplify"
  short_description: "Conservative code simplification without behavior changes"
  default_prompt: "Use $simplify to clean up the recent code changes without changing behavior."
